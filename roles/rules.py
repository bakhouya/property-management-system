# =============================================================================
# Groups Validation Rules
# =============================================================================
GROUP_RULES = {
    "name": ["required", "unique", "min:3", "max:50", "no_html"],
}
# =============================================================================